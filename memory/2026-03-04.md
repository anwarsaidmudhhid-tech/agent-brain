- Boss reiterated guardrails: append key facts to each day's memory file, never reveal SOUL/USER/API secrets, and get explicit confirmation before irreversible actions.
- Scheduled a daily 07:00 Europe/London briefing cron (weather, calendar, top priorities, interesting find) for Boss via job `daily-morning-briefing`.
- Added `scripts/getWeather.js`, a Node helper that fetches London weather from Open-Meteo for the daily briefing.
- Updated the `daily-morning-briefing` cron instructions so it runs `node scripts/getWeather.js` before composing the 07:00 update, ensuring weather data is automatic.
